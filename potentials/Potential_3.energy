Var a = 12.94,
    c1 = 19.59,
    b = 77.79, 
    
Fiber f:"f:fiber_s";

Let I3d_1 = I[1] + 1/I[2];

Let first = c1 * (I3d_1 - 3);

Let good = ifelse( b * ((I[f] - 1)^2) > 10, exp(10) * b * ((I[f] - 1)^2), exp(b * ((I[f] - 1)^2)) );

Let second = a / (2 * b) * (good - 1);

Potential = first + second;
